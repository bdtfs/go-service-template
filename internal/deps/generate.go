package deps

// Mocks for the point-of-use interfaces declared across internal/ (storage,
// clients, use cases, handlers) are generated with mockery. Run `make codegen`.
//
//go:generate ../../bin/mockery --all
